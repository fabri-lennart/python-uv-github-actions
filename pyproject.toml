[project]
name = "github-actions"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dotenv>=0.9.9",
    "duckdb>=1.5.4",
    "google-cloud-storage>=3.12.0",
    "httpx>=0.28.1",
    "pandas>=3.0.3",
]
